---
const { secondary = false, styles = "", disabled = false } = Astro.props
---

<button
  disabled={disabled}
  class={`${
    secondary ? "py-2 px-4 md:py-3 md:px-6 " : "py-3 px-6 md:py-4 md:px-8"
  } font-light   text-base md:text-xl cursor-pointer text-white rounded-lg transition-colors ${secondary ? "bg-[#262626] hover:bg-[#FFFFFF26]" : "bg-[#B30000] hover:bg-[#CC0000]"}  ${styles} ${
    disabled ? "opacity-50 cursor-not-allowed" : ""
  }`}>
  <slot />
</button>
